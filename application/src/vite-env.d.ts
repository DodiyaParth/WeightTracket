/// <reference types="vite/client" />

// `import.meta.env.MODE` (typed by vite/client above) is also 'e2e' when the
// app is started via `vite --mode e2e` (see playwright.config.ts) — see
// data/repo.ts + auth/AuthContext.tsx for what that swaps in.
