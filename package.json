{
  "name": "weighttracker-design-prototype",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Throwaway UI prototype for WeightTracker (design exploration only — not the production code).",
  "scripts": {
    "dev": "vite",
    "watch": "vite build --watch",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.2"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.8"
  }
}
